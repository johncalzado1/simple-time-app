import React from 'react';

import {Clock} from '../components/clock'

export const Home = () => {
    return(
        <div>
            <h1>Simple Clock App</h1>

            <Clock></Clock>
        </div>
    )
}